Hi <%= @user %>,

Someone has replied to one of your comments.

Your comment

<%= @your_comment.content %>

Reply

<%= @reply.content %>
- <%= @reply %>

You can view the reply <%= video_url(@your_comment.root, anchor: @reply.id) %>.

The Team at Podmedics

<%= render 'unsubscribe' %>