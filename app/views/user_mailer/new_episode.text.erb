Hi <%= @user %>,

There is a new <%= @video.specialty.name %> video.

You can view <%= "'#{@video.title}'" %> <%= link_to 'here', video_url(@video) %>.

The Team at Podmedics

<%= render 'unsubscribe' %>