<div class="note_right_column_full_note">
  <p class="note_title"><%= link_to note.cached_title, note_path(note) %></p>
  <p><%= sanitize(note.content) %></p>
  <p><%= link_to note.specialty.name, specialty_notes_path(note.specialty) %></p>
  <p><%= link_to note.specialty.category.name, category_notes_path(note.specialty.category) %></p>
</div>

<div class="video_page_notes_column_divide"></div>