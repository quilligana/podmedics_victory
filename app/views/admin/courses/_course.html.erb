<tr class='course'>
  <td><%= course.id %></td>
  <td><%= course.title %></td>
  <td><%= course.date.to_s(:long) %></td>
  <td><%= course.price %></td>
  <td><%= course.event_link %></td>
  <td>
    <ul class="actions">
      <li><%= link_to "Edit", edit_admin_course_path(course) %></li>
    </ul>
  </td>
</tr>
