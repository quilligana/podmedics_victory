<tr>
  <td><%= newsletter.created_at %></td>
  <td><%= newsletter.subject %></td>
  <td><%= newsletter.sent_at %></td>
  <td>
    <%= link_to "Edit", edit_admin_newsletter_path(newsletter) %>
    <%= link_to "Show", admin_newsletter_path(newsletter) %>
    <%= link_to "Remove", admin_newsletter_path(newsletter), method: :delete, data: { confirm: 'Are you sure?'} %>
  </td>
</tr>
